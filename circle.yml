test:
  pre:
    - ls $ANDROID_NDK
    - ls $ANDROID_HOME
  override:
    - ./autogen.sh
    - mkdir build
    - ../configure --with-android-sdk=$ANDROID_HOME && make:
        pwd: 
          build
    - ls build
