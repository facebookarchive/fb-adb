test:
  pre:
    - echo $ANDROID_NDK
  override:
    - ./autogen.sh
    - mkdir build
    - ../configure && make:
        environment:
          ANDROID_NDK: $ANDROID_NDK
        pwd: 
          build
